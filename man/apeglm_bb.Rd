% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink_apeglm.R
\name{apeglm_bb}
\alias{apeglm_bb}
\title{Beta-binomial shrinkage using apeglm}
\usage{
apeglm_bb(Y_succ, Y_tot, design, coef, ...)
}
\arguments{
\item{Y_succ}{Matrix of successes (e.g. reference allele counts).}

\item{Y_tot}{Matrix of trials (total counts).}

\item{design}{Model matrix with rows matching columns of \code{Y_succ}.}

\item{coef}{Index or name of the coefficient to shrink.}

\item{...}{Additional arguments passed to \code{apeglm::apeglm}. Must include
\code{param = cbind(theta, Y_tot)}, where \code{theta} contains externally estimated
positive beta-binomial concentration parameters, one per gene.}
}
\value{
Result object returned by \code{apeglm} containing MAP estimates and FSR.
}
\description{
Beta-binomial shrinkage using apeglm
}
\details{
Experimental fixed-effects interface, not a mixed-model shrinker.
Dispersion is never guessed. Supply \code{mle} through \code{...} for an adaptive
prior; otherwise apeglm uses its default prior. See the apeglm ASE tutorial.
Uses apeglm's R beta-binomial optimizer and rejects unsuccessful diagnostics
or invalid posterior estimates instead of returning them for inference.
}
