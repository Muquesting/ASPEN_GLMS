% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_glmm_bb.R
\name{fit_glmm_bb}
\alias{fit_glmm_bb}
\title{Fit per-gene beta-binomial GLMMs to allelic counts}
\usage{
fit_glmm_bb(
  sce,
  formula_fixed = ~sex + age + celltype_new + sex:age,
  rand = "(1|sample)",
  family = glmmTMB::betabinomial(link = "logit"),
  min_trials = 5,
  min_cells = 50,
  ncores = 1
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with \code{a1} and \code{tot} assays.}

\item{formula_fixed}{Fixed-effects portion supplied as a formula (rhs only).}

\item{rand}{Random-effects term (string) appended to the model.}

\item{family}{Family passed to \code{glmmTMB} (defaults to beta-binomial with logit link).}

\item{min_trials}{Minimum trials per cell used during filtering.}

\item{min_cells}{Minimum cells meeting the trial threshold per gene.}

\item{ncores}{Number of workers for parallel fitting (\code{future.apply}).}
}
\value{
A tibble with one row per gene/term combination containing estimates.
}
\description{
Fit per-gene beta-binomial GLMMs to allelic counts
}
\details{
The function filters genes by coverage, fits \code{glmmTMB} models and
returns Wald statistics. Covariates must exist in \code{colData(sce)} and be
complete. Optimizer success, a positive-definite Hessian and finite
coefficient statistics are required. Genes failing these checks are reported with
\code{converged = FALSE} and contain an error message in the \code{error} column.
}
